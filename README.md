
  # Prior Authorization Dashboard

  This is a code bundle for Prior Authorization Dashboard. The original project is available at https://www.figma.com/design/n5fSTH6fInWMCahNsb51dJ/Prior-Authorization-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  